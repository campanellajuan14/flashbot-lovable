
import ShareSettings from './share/ShareSettings';

export { ShareSettings };

// Re-export the ShareSettings interface for use in other components
export type { ShareSettings as ShareSettingsType } from './share/types';

export default ShareSettings;
